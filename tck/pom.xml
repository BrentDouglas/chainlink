<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>chainlink-parent</artifactId>
        <groupId>io.machinecode.chainlink</groupId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>chainlink-tck</artifactId>
    <packaging>pom</packaging>
    <modules>
        <module>core</module>
    </modules>

    <properties>
        <test.properties>../test.properties</test.properties>

        <configuration.factory.type>Memory</configuration.factory.type>
        <database.driver>org.h2.Driver</database.driver>
        <database.prefix>h2</database.prefix>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.machinecode.chainlink</groupId>
                <artifactId>chainlink-tck-core</artifactId>
                <scope>test</scope>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.ibm.jbatch</groupId>
            <artifactId>com.ibm.jbatch-tck-spi</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>com.ibm.jbatch</groupId>
                    <artifactId>com.ibm.jbatch-model</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.ibm.jbatch</groupId>
                    <artifactId>com.ibm.jbatch-ri-spi</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.ibm.jbatch</groupId>
                    <artifactId>com.ibm.jbatch-runtime</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.sf.trove4j</groupId>
            <artifactId>trove4j</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Memory -->
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-memory</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Infinispan -->
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-infinispan</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.infinispan</groupId>
            <artifactId>infinispan-core</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Redis -->
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-redis</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>redis.clients</groupId>
            <artifactId>jedis</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Jdbc -->
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-jdbc</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Jpa -->
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-jpa</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-entitymanager</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>infinispan</id>
            <activation>
                <property><name>infinispan</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <configuration.factory.type>Infinispan</configuration.factory.type>
            </properties>
        </profile>
        <profile>
            <id>jdbc</id>
            <activation>
                <property><name>jdbc</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <configuration.factory.type>Jdbc</configuration.factory.type>
            </properties>
        </profile>
        <profile>
            <id>jpa</id>
            <activation>
                <property><name>jpa</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <configuration.factory.type>Jpa</configuration.factory.type>
            </properties>
        </profile>
        <profile>
            <id>redis</id>
            <activation>
                <property><name>redis</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <configuration.factory.type>Redis</configuration.factory.type>
            </properties>
        </profile>
        <profile>
            <id>all</id>
            <activation>
                <property><name>all</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <modules>
                <module>batch</module>
                <module>cdi</module>
                <module>seam</module>
                <module>spring</module>
            </modules>
        </profile>
        <profile>
            <id>batch</id>
            <activation>
                <property><name>batch</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <modules>
                <module>batch</module>
            </modules>
        </profile>
        <profile>
            <id>cdi</id>
            <activation>
                <property><name>cdi</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <modules>
                <module>cdi</module>
            </modules>
        </profile>
        <profile>
            <id>guice</id>
            <activation>
                <property><name>guice</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <modules>
                <module>guice</module>
            </modules>
        </profile>
        <profile>
            <id>seam</id>
            <activation>
                <property><name>seam</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <modules>
                <module>seam</module>
            </modules>
        </profile>
        <profile>
            <id>spring</id>
            <activation>
                <property><name>spring</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <modules>
                <module>spring</module>
            </modules>
        </profile>

        <!-- JPA -->
        <profile>
            <id>mariadb</id>
            <activation>
                <property><name>mariadb</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <database.driver>org.mariadb.jdbc.Driver</database.driver>
                <database.prefix>mariadb</database.prefix>
            </properties>
        </profile>
        <profile>
            <id>postgresql</id>
            <activation>
                <property><name>postgresql</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <database.driver>org.postgresql.Driver</database.driver>
                <database.prefix>postgresql</database.prefix>
            </properties>
        </profile>
    </profiles>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>${version.org.apache.maven.plugins.maven-antrun-plugin}</version>
                    <executions>
                        <execution>
                            <id>create-persistence-xml</id>
                            <goals>
                                <goal>run</goal>
                            </goals>
                            <phase>generate-test-resources</phase>
                            <configuration>
                                <target>
                                    <copy file="${project.basedir}/src/test/resources/META-INF/persistence.xml.template"
                                          tofile="${project.basedir}/src/test/resources/META-INF/persistence.xml"
                                          filtering="true"
                                          overwrite="true">
                                        <filterset>
                                            <filter token="database.url" value="${database.prefix}.database.url" />
                                            <filter token="database.user" value="${database.prefix}.database.user" />
                                            <filter token="database.password" value="${database.prefix}.database.password" />
                                        </filterset>
                                    </copy>
                                </target>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>properties-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>read-project-properties</goal>
                        </goals>
                        <configuration>
                            <files>
                                <file>${test.properties}</file>
                            </files>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>add-test-sources</id>
                        <phase>generate-test-sources</phase>
                        <goals><goal>add-test-source</goal></goals>
                        <configuration>
                            <sources>
                                <source>${tck.source}/src</source>
                                <source>${tck.source}/testArtifacts</source>
                            </sources>
                        </configuration>
                    </execution>
                    <execution>
                        <id>add-test-resources</id>
                        <phase>generate-test-resources</phase>
                        <goals><goal>add-test-resource</goal></goals>
                        <configuration>
                            <resources>
                                <resource>
                                    <directory>${tck.source}/testJobXml/META-INF/batch-jobs</directory>
                                    <targetPath>META-INF/batch-jobs</targetPath>
                                </resource>
                                <resource>
                                    <directory>${project.basedir}/src/test/resources</directory>
                                </resource>
                                <resource>
                                    <directory>${project.basedir}/../../spi/src/main/resources</directory>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <includes>
                        <include>**/**Test*.java</include>
                        <include>**/**TestCase*.java</include>
                    </includes>
                    <systemPropertiesFile>${test.properties}</systemPropertiesFile>
                    <systemPropertyVariables>
                        <database.driver>${database.driver}</database.driver>
                        <database.prefix>${database.prefix}</database.prefix>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>