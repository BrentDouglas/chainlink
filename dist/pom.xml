<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>chainlink-parent</artifactId>
        <groupId>io.machinecode.chainlink</groupId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>chainlink-dist</artifactId>
    <packaging>pom</packaging>

    <dependencies>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-ee-glassfish</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-ee-tomee</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-ee-wildfly</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-jsl-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-jsl-fluent</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-jsl-groovy</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-jsl-xml</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-inject-cdi</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-inject-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-inject-guice</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-inject-jndi</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-inject-spring</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-inject-seam</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-marshalling-jboss</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-marshalling-jdk</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-ehcache</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-gridgain</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-hazelcast</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-infinispan</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-memory</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-mongo</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-repository-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-se</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-server-chainlinkd</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-transport-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-transport-gridgain</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-transport-hazelcast</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-transport-infinispan</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.chainlink</groupId>
            <artifactId>chainlink-transport-jgroups</artifactId>
        </dependency>

        <dependency>
            <groupId>com.google.inject</groupId>
            <artifactId>guice</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>
        <dependency>
            <groupId>com.hazelcast</groupId>
            <artifactId>hazelcast</artifactId>
        </dependency>
        <dependency>
            <groupId>gnu-getopt</groupId>
            <artifactId>getopt</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.then</groupId>
            <artifactId>then-api</artifactId>
        </dependency>
        <dependency>
            <groupId>io.machinecode.then</groupId>
            <artifactId>then-core</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.enterprise</groupId>
            <artifactId>cdi-api</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.inject</groupId>
            <artifactId>javax.inject</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>servlet-api</artifactId>
        </dependency>
        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
        </dependency>
        <dependency>
            <groupId>net.sf.ehcache</groupId>
            <artifactId>ehcache-core</artifactId>
        </dependency>
        <dependency>
            <groupId>net.sf.trove4j</groupId>
            <artifactId>trove4j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.gridgain</groupId>
            <artifactId>gridgain-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.gridgain</groupId>
            <artifactId>gridgain-indexing</artifactId>
        </dependency>
        <dependency>
            <groupId>org.hibernate.javax.persistence</groupId>
            <artifactId>hibernate-jpa-2.0-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-entitymanager</artifactId>
        </dependency>
        <dependency>
            <groupId>org.infinispan</groupId>
            <artifactId>infinispan-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jboss.logging</groupId>
            <artifactId>jboss-logging</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jboss.marshalling</groupId>
            <artifactId>jboss-marshalling</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jboss.marshalling</groupId>
            <artifactId>jboss-marshalling-river</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jboss.seam</groupId>
            <artifactId>jboss-seam</artifactId>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>org.jboss.spec.javax.transaction</groupId>
            <artifactId>jboss-transaction-api_1.1_spec</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jboss.weld.se</groupId>
            <artifactId>weld-se</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jgroups</groupId>
            <artifactId>jgroups</artifactId>
        </dependency>
        <dependency>
            <groupId>org.jongo</groupId>
            <artifactId>jongo</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mariadb.jdbc</groupId>
            <artifactId>mariadb-java-client</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mongodb</groupId>
            <artifactId>mongo-java-driver</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-beans</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
        </dependency>
        <dependency>
            <groupId>postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>redis.clients</groupId>
            <artifactId>jedis</artifactId>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>coherence</id>
            <activation>
                <property><name>coherence</name></property>
                <activeByDefault>false</activeByDefault>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>io.machinecode.chainlink</groupId>
                    <artifactId>chainlink-repository-coherence</artifactId>
                </dependency>
                <dependency>
                    <groupId>io.machinecode.chainlink</groupId>
                    <artifactId>chainlink-transport-coherence</artifactId>
                </dependency>
                <!--
                <dependency>
                    <groupId>com.oracle.coherence</groupId>
                    <artifactId>coherence</artifactId>
                </dependency>
                -->
            </dependencies>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>${version.org.apache.maven.plugins.maven-antrun-plugin}</version>
                <executions>
                    <execution>
                        <id>ensure-test-properties</id>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <target>
                                <copy file="${project.basedir}/vars"
                                      tofile="${project.build.directory}/vars"
                                      filtering="true"
                                      overwrite="true">
                                    <filterset>
                                        <filter token="version.io.machinecode.chainlink.chainlink-api" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-core" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-ee-glassfish" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-ee-tomee" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-ee-wildfly" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-inject-core" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-inject-cdi" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-inject-guice" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-inject-seam" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-inject-spring" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-jsl-core" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-jsl-fluent" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-jsl-groovy" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-jsl-xml" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-marshalling-jboss" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-marshalling-jdk" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-core" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-coherence" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-ehcache" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-gridgain" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-hazelcast" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-infinispan" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-jdbc" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-jpa" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-memory" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-mongo" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-repository-redis" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-se" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-server-chainlinkd" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-spi" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-transport-core" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-transport-coherence" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-transport-gridgain" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-transport-hazelcast" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-transport-infinispan" value="${project.version}" />
                                        <filter token="version.io.machinecode.chainlink.chainlink-transport-jgroups" value="${project.version}" />

                                        <filter token="version.com.google.inject.guice" value="${version.com.google.inject.guice}" />
                                        <filter token="version.com.h2database.h2" value="${version.com.h2database.h2}" />
                                        <filter token="version.com.hazelcast.hazelcast" value="${version.com.hazelcast.hazelcast}" />
                                        <filter token="version.com.oracle.coherence.coherence" value="${version.com.oracle.coherence.coherence}" />
                                        <filter token="version.gnu-getopt.getopt" value="${version.gnu-getopt.getopt}" />
                                        <filter token="version.io.machinecode.then.then-api" value="${version.io.machinecode.then}" />
                                        <filter token="version.io.machinecode.then.then-core" value="${version.io.machinecode.then}" />
                                        <filter token="version.javax.enterprise.cdi-api" value="${version.javax.enterprise.cdi-api}" />
                                        <filter token="version.javax.inject.javax.inject" value="${version.javax.inject}" />
                                        <filter token="version.javax.servlet.servlet-api" value="${version.javax.servlet.servlet-api}" />
                                        <filter token="version.log4j.log4j" value="${version.log4j.log4j}" />
                                        <filter token="version.net.sf.ehcache.ehcache-core" value="${version.net.sf.ehcache.ehcache-core}" />
                                        <filter token="version.net.sf.trove4j.trove4j" value="${version.net.sf.trove4j.trove4j}" />
                                        <filter token="version.org.gridgain.gridgain-core" value="${version.org.gridgain.gridgain-core}" />
                                        <filter token="version.org.gridgain.gridgain-indexing" value="${version.org.gridgain.gridgain-indexing}" />
                                        <filter token="version.org.hibernate.hibernate-core" value="${version.org.hibernate}" />
                                        <filter token="version.org.hibernate.hibernate-entitymanager" value="${version.org.hibernate}" />
                                        <filter token="version.org.infinispan.infinispan-commons" value="${version.org.infinispan.infinispan-bom}" />
                                        <filter token="version.org.infinispan.infinispan-core" value="${version.org.infinispan.infinispan-bom}" />
                                        <filter token="version.org.jboss.logging.jboss-logging" value="${version.org.jboss.logging.jboss-logging}" />
                                        <filter token="version.org.jboss.marshalling.jboss-marshalling" value="${version.org.jboss.marshalling.jboss-marshalling}" />
                                        <filter token="version.org.jboss.marshalling.jboss-marshalling-river" value="${version.org.jboss.marshalling.jboss-marshalling}" />
                                        <filter token="version.org.jboss.seam.jboss-seam" value="${version.org.jboss.seam.jboss-seam}" />
                                        <filter token="version.org.jboss.weld.se.weld-se" value="${version.org.jboss.weld.se.weld-se}" />
                                        <filter token="version.org.jgroups.jgroups" value="${version.org.jgroups.jgroups}" />
                                        <filter token="version.org.jongo.jongo" value="${version.org.jongo.jongo}" />
                                        <filter token="version.org.mariadb.jdbc.mariadb-java-client" value="${version.org.mariadb.jdbc.mariadb-java-client}" />
                                        <filter token="version.org.mongodb.mongo-java-driver" value="${version.org.mongodb.mongo-java-driver}" />
                                        <filter token="version.org.springframework.spring-beans" value="${version.org.springframework.spring-beans}" />
                                        <filter token="version.org.springframework.spring-context" value="${version.org.springframework.spring-context}" />
                                        <filter token="version.org.springframework.spring-core" value="${version.org.springframework.spring-core}" />
                                        <filter token="version.postgresql.postgresql" value="${version.postgresql.postgresql}" />
                                        <filter token="version.redis.clients.jedis" value="${version.redis.clients.jedis}" />
                                    </filterset>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <executions>
                    <execution>
                        <id>assemble</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <descriptors>
                                <descriptor>all.xml</descriptor>
                                <descriptor>min.xml</descriptor>
                                <descriptor>src.xml</descriptor>
                            </descriptors>
                            <finalName>chainlink-${project.version}</finalName>
                            <appendAssemblyId>true</appendAssemblyId>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <workDirectory>${project.build.directory}/assembly/work</workDirectory>
                            <tarLongFileMode>gnu</tarLongFileMode>
                        </configuration>
                    </execution>
                </executions>
                <inherited>false</inherited>
            </plugin>
        </plugins>
    </build>

</project>